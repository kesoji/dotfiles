gui:
  language: "en"
git:
  #pagers:
  #  - pager: delta --paging=never --side-by-side
  #    colorArg: always
  diffRenderers:
    - colorArg: always
      command: delta --paging=never --side-by-side
keybinding:
  universal:
    quit: q #sample
